import type { Metadata } from "next";
import { Halant } from "next/font/google";
import { Inter } from "next/font/google";
import "./globals.css";
import "@/lib/gsap-setup";
import { ServiceWrapper } from "@/components/ServiceWrapper";
import Tag from "@/tag/Tag";
import { getVisualEditScript } from "@/utils/visual-edit-script";

const halant = Halant({
  variable: "--font-halant",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"],
});

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = { title: 'Kings Market | Premium Neighborhood Grocery in San Francisco', description: 'Experience the best of the Mission District at Kings Market. Fresh produce, local artisans, and deli essentials delivered to your door. Open daily on Bryant St.', openGraph: { title: 'Kings Market | Premium Neighborhood Grocery in San Francisco', description: 'Experience the best of the Mission District at Kings Market. Fresh produce, local artisans, and deli essentials delivered to your door.', type: 'website', url: 'https://kingsmarket.sf' }, robots: { index: true, follow: true } };

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <ServiceWrapper>
        <body
          className={`${halant.variable} ${inter.variable} antialiased`}
        >
          <Tag />
          {children}
          <script
              dangerouslySetInnerHTML={{
                  __html: `${getVisualEditScript()}`
              }}
          />
        </body>
      </ServiceWrapper>
    </html>
  );
}
